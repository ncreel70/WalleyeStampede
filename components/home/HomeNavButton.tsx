import React from "react";
import { TouchableOpacity, View, Text } from "react-native";

const HomeNavButton = ({ title }) => {
  <View>
    <TouchableOpacity>
      <Text>{title}</Text>
    </TouchableOpacity>
  </View>;
};

export default HomeNavButton;
